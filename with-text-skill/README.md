# with-text-skill

Tests agent performance when a simple plain-text skill is provided that explains how SvelteKit remote functions work — covering queries, commands, forms, and single-flight mutations. This tests whether lightweight textual instruction is sufficient for an agent to correctly implement a feature outside its training data.
