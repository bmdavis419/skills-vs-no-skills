# before-agents

Baseline control project — the starting state before any AI agent touched it. The weather service is set up and working, but no UI exists yet. This is the reference point for comparing how well agents implement SvelteKit remote functions under different context conditions.
