# without-skill

Control group — the agent receives only the task prompt with no skills, MCPs, or example code. Tests the model's raw ability to implement SvelteKit remote functions (a feature outside its training data) from the prompt alone.
